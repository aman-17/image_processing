<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<cat xmlns="urn:apple:names:siri:cat:1.0" id="SearchCallHistory#UnsupportedUnseen" locale="en">
    <conditions>
        <condition name="isRequestForVoicemail" note="Is request for voicemail calls">
          <expression name="intent.isRequestForVoicemail"/>
        </condition>
    </conditions>
    <first>
        <dialog id="isRequestForVoicemail" condition="isRequestForVoicemail">Sorry, I can’t get your new voicemails.</dialog>
        <dialog>Sorry, I can’t show your new calls.</dialog>
    </first>
</cat>
